apiVersion: v1
kind: Service
metadata:
  name: {{ include "deployment-app.fullname" . }}
  labels:
    {{- include "deployment-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deployment-app.selectorLabels" . | nindent 4 }}
